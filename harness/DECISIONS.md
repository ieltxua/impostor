# DECISIONS - Impostor Web MVP

Last updated: 2026-02-23

## Decision log

| ID | Date | Decision | Status | Rationale |
|---|---|---|---|---|
| D-001 | 2026-02-21 | MVP excludes embedded audio/video. | Accepted | Keeps MVP focused on game state and UX safety; remote play uses external call. |
| D-002 | 2026-02-21 | Undercover role is optional (`0..n`). | Accepted | Supports both 1-word and 2-word play styles with one rules engine. |
| D-003 | 2026-02-21 | Elimination reveals role only, not the assigned word. | Accepted | Preserves deduction tension and prevents late leaks. |
| D-004 | 2026-02-21 | LIVE mode enforces strict post-Reveal secret lock. | Accepted | Anti-spoiler guarantee is core MVP acceptance criterion. |
| D-005 | 2026-02-21 | Server-authoritative state machine and timer. | Accepted | Prevents client drift and simplifies correctness under reconnects. |
| D-006 | 2026-02-21 | Tie on top vote resolves by random elimination among tied players. | Accepted | Fast low-friction flow for MVP without extra tie-break rounds. |
| D-007 | 2026-02-21 | Local Mac host plus tunnel is default deployment target. | Accepted | Zero-cost path for real gameplay testing. |
| D-008 | 2026-02-21 | Use project-local subagent routing (`explorer_fast`, `implementer_balanced`, `reviewer_deep`). | Accepted | Improves repeatability for discovery/implementation/review cycles. |
| D-009 | 2026-02-21 | Harness docs are required for meaningful changes. | Accepted | Keeps decisions, pending work, and change history explicit. |
| D-010 | 2026-02-21 | Host-only socket actions require both host identity and `hostKey`. | Accepted | Prevents simple player-id/session spoofing from triggering host actions. |
| D-011 | 2026-02-21 | Vote auto-close uses connected alive players to avoid deadlocks on disconnect. | Accepted | Keeps rounds moving when a voter drops mid-vote. |
| D-012 | 2026-02-21 | Pilot tunnel provider priority is Cloudflare, then ngrok, then localtunnel. | Accepted | Balances setup friction, reliability, and free-tier usability for MVP playtests. |
| D-013 | 2026-02-21 | Remote pilot default mode is single-origin (`server + built client` on `3000`) instead of Vite dev sharing. | Accepted | Avoids dual-tunnel/env wiring errors and keeps invite links + socket origin consistent for external players. |
| D-014 | 2026-02-21 | Reliability baseline includes automated socket simulation (`sim:pilot`) before relying on manual pilot sessions. | Accepted | Enables repeatable multiplayer stress coverage without coordinating 6-10 humans every run. |
| D-015 | 2026-02-21 | UI smoke tests must target stable `data-testid` contracts, not layout/text styling. | Accepted | Preserves test value through planned visual redesign with minimal maintenance. |
| D-016 | 2026-02-21 | Playwright smoke runs on dedicated test ports (`5174` client -> `3100` server) and starts fresh servers each run. | Accepted | Prevents false confidence from stale pre-running dev processes and avoids conflicts with local manual sessions on `5173/3000`. |
| D-017 | 2026-02-21 | Host can explicitly close/delete the room and return to Home from lobby. | Accepted | Gives fast restart path when room setup is wrong and guarantees stale room cleanup server-side. |
| D-018 | 2026-02-21 | Client visual system uses global tokenized CSS + `data-theme` root switching (`light`, `dark`) while preserving existing gameplay/test selectors. | Accepted | Enables premium light/dark redesign and accessibility without changing game logic or breaking selector-based smoke tests. |
| D-019 | 2026-02-21 | Theme selector uses native button semantics with `aria-pressed` state instead of custom radio roles. | Accepted | Avoids incomplete custom radiogroup keyboard behavior while preserving accessible state announcement and simple interaction. |
| D-020 | 2026-02-21 | Theme selector scope is limited to explicit light/dark options only (no user-facing Auto mode). | Accepted | Matches UX request for only two selectable modes while keeping predictable, low-friction controls. |
| D-021 | 2026-02-21 | Capture redesign exploration as three generated visual concept boards before implementation changes. | Accepted | De-risks UI direction by enabling side-by-side selection before touching component code and theme tokens. |
| D-022 | 2026-02-21 | Random word source accepts both `categories[]` and legacy `category`, with server fallback to full deck when filters miss. | Accepted | Enables new host multi-category deck filtering while preserving backward compatibility and avoiding empty-deck game start failures. |
| D-023 | 2026-02-21 | Current UI direction is a game-first “command deck” visual language (HUD panels, scanline/grid atmosphere, high-contrast status cues) for both light/dark themes. | Accepted | Addresses feedback that prior styling felt generic and increases thematic clarity without changing gameplay behavior or test contracts. |
| D-024 | 2026-02-21 | Visual A/B testing uses route-prefixed variants (`/v1`, `/v3`) sharing the same game logic and selectors. | Accepted | Enables side-by-side theme evaluation without code-branching gameplay behavior or test contracts. |
| D-025 | 2026-02-21 | Invite QR codes are generated client-side with local library (`qrcode`) rather than external QR API. | Accepted | Avoids third-party runtime dependency, keeps share links private, and works consistently offline/local. |
| D-026 | 2026-02-22 | Client reconnect should ignore stale room-not-found errors targeting a different room code and keep invite links join-first by default. | Accepted | Prevents lobby+error mismatch and accidental room creation by invited players while preserving reliable reconnect behavior. |
| D-027 | 2026-02-22 | Host may manage local no-device players (lobby add/remove, local reveal requests, and host-cast local votes) while preserving socket-based remote players. | Accepted | Enables in-person play without forcing one device per person and keeps existing remote join flow unchanged. |
| D-028 | 2026-02-22 | Block `CLASSIC_GUESS` + `mrWhiteCanGuessOnElim` when local no-device players are present. | Accepted | Prevents unreachable Mr White guess prompt flow for local players with no socket endpoint. |
| D-029 | 2026-02-22 | Host manual voting is generalized to any alive player and host can trigger `Next Word` to restart immediately from active game phases. | Accepted | Keeps live sessions non-blocking when table players skip app voting and lets host continue play without forcing full room reset. |
| D-030 | 2026-02-22 | Lobby local player management is full-cycle: add/remove/rename local-only participants, with auto role-count derivation (`civil = total - undercover - mrWhite`). | Accepted | Improves physical room setup ergonomics and prevents local player role configuration drift as seat count changes. |
| D-031 | 2026-02-22 | Local-only players are treated as ready-equivalent for start validation. | Accepted | Eliminates false role-count mismatch in local-seat workflows and keeps start/ready semantics clear for physical-table mode. |
| D-032 | 2026-02-22 | Host start action re-syncs current lobby settings before launching game. | Accepted | Prevents `INVALID_STATE` mismatches caused by stale/unapplied settings between UI changes and immediate start attempts, especially with local player-heavy lobbies. |
| D-033 | 2026-02-22 | LIVE reveal should support explicit host-driven turn order across connected players. | Accepted | Supports physical-device pass-and-reveal flow and prevents accidental out-of-turn access to secrets. |
| D-034 | 2026-02-22 | Random-word game setup should preserve room language preference at session start. | Accepted | Keeps gameplay immersion by localizing draw-by-draw pairs automatically for Spanish users. |
| D-035 | 2026-02-22 | Reveal UX on shared device should stay minimal and avoid exposing next-player queue messaging. | Accepted | Reduces confusion and visible leakage while preserving host-guided anti-spoiler progression. |
| D-036 | 2026-02-22 | Host-local secret previews should be reset when moving reveal order forward. | Accepted | Prevents stale personal role/word data from remaining visible when the next player takes over the shared device. |
| D-037 | 2026-02-22 | LIVE reveal must be locked by `currentRevealPlayerId` so only the active turn holder can reveal a card. | Accepted | Stops cross-player peeking when devices are handed off and enforces turn-by-turn secret exposure in physical-table sessions. |
| D-038 | 2026-02-22 | Hide host-only local-secret discovery paths during LIVE reveal. | Accepted | Blocks alternate "host reveal" UI pathways that could leak assigned secrets while passing one shared device between players. |
| D-039 | 2026-02-22 | In shared-device LIVE, show only the active turn holder's card and display reveal attempt counts. | Accepted | Reduces possibility of peeking and gives the host a verifiable turn-by-turn audit trail in physical sessions. |
| D-040 | 2026-02-22 | In LIVE with local-only participants, emit `player:secret` only for the current reveal holder. | Accepted | Prevents host (single socket) from receiving all local participants' roles at once while keeping full broadcast behavior for regular multi-device sessions. |
| D-041 | 2026-02-22 | In LIVE sessions without local-only participants, `player:secret` events must be envelope-bound and filtered client-side. | Accepted | Ensures each disconnected/reconnected device receives only its own role/word secret and cannot be overwritten by another player's broadcast event. |
| D-042 | 2026-02-22 | Ready-state and vote-state UX should be explicit and authoritative-perceived, not inferred from host/local assumptions. | Accepted | Uses authoritative readiness from `player.ready` for display/gating and explicit vote target confirmation text so users can verify their action on each device. |
| D-043 | 2026-02-22 | Resolve flow should surface explicit role-removal outcome and game-continuation CTA in a modal. | Accepted | Players need immediate clarity after voting, including whether the eliminated role was Civil/Infiltrado/Mr White and whether the host should continue the round or begin a new word. |
| D-044 | 2026-02-22 | Default word deck variants should avoid localized near-synonyms. | Accepted | Prevents rounds from being trivialized by pairs that map to highly similar words in common locales (e.g., sofá/sillón). |
| D-045 | 2026-02-22 | Category labels should be localized alongside random word pairs. | Accepted | Keeps language consistency for both game setup and game-end summaries, avoiding mixed-environment category values in Spanish sessions. |
| D-046 | 2026-02-22 | Host disconnect is treated as temporary first (`HOST_DISCONNECTED_TEMPORARY`) with reconnect grace; stale socket disconnects are ignored. | Accepted | Prevents false room deletion and reconnect loops (`ROOM_NOT_FOUND`) when host/devices refresh while preserving intentional close behavior via `host:closeRoom`. |
| D-047 | 2026-02-22 | Lobby start validation is based on active-ready participants only (`connected || local`). | Accepted | Prevents disconnected players from being included in role assignment/start checks, reducing false lobby consistency issues on mobile reconnects. |
| D-048 | 2026-02-22 | Resolve advance should continue same-word clues after non-winning eliminations; next-word restart is explicit. | Accepted | Avoids accidental word resets after eliminating a civilian and matches expected round progression. |
| D-049 | 2026-02-22 | In-room chrome is compact-first for mobile sessions and players get a quick personal secret re-check action after reveal. | Accepted | Reduces cognitive load in 95% phone usage while preserving critical game controls only. |
| D-050 | 2026-02-22 | Invite-link home state is join-first and should not present create-room at equal visual priority. | Accepted | Most real usage enters through shared links/QR; a single obvious action lowers join errors and speeds room entry. |
| D-051 | 2026-02-22 | Timed clue rounds support host-only pause/resume with shared paused indicator. | Accepted | Keeps turn flow controllable during real-world interruptions while preserving synchronized timer visibility for every player. |
| D-052 | 2026-02-23 | Canonical invite URL format is route-based (`/join/:roomCode`) while query-based invites remain backward-compatible. | Accepted | Makes invite entry clearer/mobile-first and avoids exposing a second room-code input path when users arrive from QR/link. |
| D-053 | 2026-02-23 | Presence authority is heartbeat + stale timeout, not disconnect events alone. | Accepted | Mobile browsers can keep sockets in ambiguous states; heartbeat sweep makes reconnecting/offline indicators consistent across players. |
| D-054 | 2026-02-23 | Discussion phase should be speaker-first with one dominant host control action. | Accepted | In live family play, users need one focal element (current speaker) and minimal UI noise to keep turn flow intuitive on phones. |
| D-055 | 2026-02-23 | LIVE mixed lobbies (local + remote devices) use split reveal/vote responsibilities. | Accepted | Connected players must reveal/vote on their own devices, while host only manages local no-device participants to avoid blocking mixed sessions. |
| D-056 | 2026-02-23 | Mixed LIVE reveal order must include host plus local no-device players. | Accepted | Ensures host has an explicit reveal turn in mixed sessions while keeping remote-device players on self-reveal flow. |
| D-057 | 2026-02-23 | Reveal phase can only close after all alive players open secret at least once. | Accepted | Prevents premature host close and guarantees minimum reveal completion for both shared-device and multi-device sessions. |
| D-058 | 2026-02-23 | Host can force next clue turn regardless of timer mode. | Accepted | Keeps live sessions moving when players finish early while preserving optional pause/resume timer control. |
| D-059 | 2026-02-23 | Host disconnect on refresh should keep room alive through grace window, including host-only rooms. | Accepted | Allows host token/session rejoin after accidental refresh instead of immediate room deletion. |
| D-060 | 2026-02-23 | Post-start rejoin is restricted to previous participants, empty rooms expire after 1 hour, and resumes after >=10 idle minutes require explicit host continue/close. | Accepted | Matches mobile tabletop pauses without reopening active matches to new players and prevents silent continuation after long idle gaps. |
| D-061 | 2026-02-23 | Setup UX is random-deck-only with category filtering, no mode selector in create flow, and default no-timer turns. | Accepted | Reduces setup friction for mobile-first family play and enforces one consistent, low-cognitive-load room configuration path. |
| D-062 | 2026-02-23 | During a running match, host can close room immediately and can schedule host transfer to a connected device player, applied at next-round start with regenerated host key. | Accepted | Gives operational control for live sessions (hard stop/recovery) while preserving host-action security and avoiding mid-round authority flips. |

## Open decisions
- O-001: Keep room code after reset or issue a new code.
- O-002: Whether to expose vote tally in real-time (`showVoteTally`) by default.
- O-003: Whether Mr White guess mode (`CLASSIC_GUESS`) ships enabled or behind host toggle.
